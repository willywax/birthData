import database from '../database/models';

const { Countries, LifeExpectancies } = database;


class DataService {

    async createCountry(){
        try {
            
        } catch (error) {
            throw error;
        }
    }

    async

}